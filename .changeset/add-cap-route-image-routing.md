---
"@moonshot-ai/kimi-code": minor
---

Add image capability routing: set `[cap_route] image_route` in config.toml to a vision model alias so images are sent to that model and replaced with its text descriptions when the current model cannot see images.
